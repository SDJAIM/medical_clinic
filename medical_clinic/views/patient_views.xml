<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_clinic_patient_tree" model="ir.ui.view">
        <field name="name">clinic.patient.tree</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="patient_code"/>
                <field name="full_name"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="last_visit_date"/>
                <field name="state" widget="badge" decoration-success="state == 'active'" decoration-danger="state == 'deceased'"/>
            </tree>
        </field>
    </record>
    
    <!-- Form View -->
    <record id="view_clinic_patient_form" model="ir.ui.view">
        <field name="name">clinic.patient.form</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <form string="Patient">
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="active,inactive"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_appointments" type="object" class="oe_stat_button" icon="fa-calendar">
                            <field name="appointment_count" widget="statinfo" string="Appointments"/>
                        </button>
                        <button name="action_view_treatments" type="object" class="oe_stat_button" icon="fa-stethoscope">
                            <field name="treatment_count" widget="statinfo" string="Treatments"/>
                        </button>
                    </div>
                    <field name="image" widget="image" class="oe_avatar" options="{'size': [90, 90]}"/>
                    <div class="oe_title">
                        <label for="patient_code" string="Patient Code"/>
                        <h1><field name="patient_code" readonly="1"/></h1>
                        <group>
                            <group>
                                <field name="first_name"/>
                                <field name="last_name"/>
                                <field name="date_of_birth"/>
                                <field name="age"/>
                            </group>
                            <group>
                                <field name="gender"/>
                                <field name="blood_group"/>
                                <field name="phone"/>
                                <field name="email" widget="email"/>
                            </group>
                        </group>
                    </div>
                    <notebook>
                        <page string="Medical Information">
                            <group>
                                <group string="Medical History">
                                    <field name="allergies" placeholder="Enter known allergies..."/>
                                    <field name="chronic_conditions" placeholder="Enter chronic conditions..."/>
                                    <field name="current_medications" placeholder="Enter current medications..."/>
                                </group>
                                <group string="Family History">
                                    <field name="family_history" placeholder="Enter family medical history..."/>
                                    <field name="notes" placeholder="Additional notes..."/>
                                </group>
                            </group>
                        </page>
                        <page string="Contact Information">
                            <group>
                                <group string="Address">
                                    <field name="street" placeholder="Street..."/>
                                    <field name="street2" placeholder="Street 2..."/>
                                    <field name="city" placeholder="City"/>
                                    <field name="state_id" options="{'no_create': True}"/>
                                    <field name="country_id" options="{'no_create': True}"/>
                                    <field name="zip" placeholder="ZIP"/>
                                </group>
                                <group string="Emergency Contact">
                                    <field name="emergency_contact"/>
                                    <field name="emergency_phone"/>
                                    <field name="emergency_relation"/>
                                </group>
                            </group>
                        </page>
                        <page string="Insurance">
                            <field name="insurance_ids">
                                <tree>
                                    <field name="insurance_company_id"/>
                                    <field name="policy_number"/>
                                    <field name="is_primary" widget="boolean_toggle"/>
                                    <field name="is_active" widget="badge" decoration-success="is_active" decoration-danger="not is_active"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Documents">
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>
    
    <!-- Search View -->
    <record id="view_clinic_patient_search" model="ir.ui.view">
        <field name="name">clinic.patient.search</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <search>
                <field name="patient_code"/>
                <field name="first_name"/>
                <field name="last_name"/>
                <field name="phone"/>
                <field name="email"/>
                <filter name="active_patients" string="Active" domain="[('state', '=', 'active')]"/>
                <filter name="has_insurance" string="Has Insurance" domain="[('insurance_ids', '!=', False)]"/>
                <separator/>
                <filter name="visited_this_month" string="Visited This Month" 
                        domain="[('last_visit_date', '&gt;=', (datetime.datetime.now() - relativedelta(months=1)).strftime('%Y-%m-01'))]"/>
                <group string="Group By">
                    <filter name="group_by_gender" string="Gender" context="{'group_by': 'gender'}"/>
                    <filter name="group_by_blood_group" string="Blood Group" context="{'group_by': 'blood_group'}"/>
                    <filter name="group_by_state" string="Status" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>
    
    <!-- Kanban View -->
    <record id="view_clinic_patient_kanban" model="ir.ui.view">
        <field name="name">clinic.patient.kanban</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="full_name"/>
                <field name="patient_code"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="image"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('clinic.patient', 'image', record.id.raw_value)" 
                                     t-att-alt="record.full_name.value" width="48" height="48"/>
                            </div>
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="full_name"/>
                                </strong>
                                <div class="text-muted">
                                    <field name="patient_code"/>
                                </div>
                                <div>
                                    <span class="text-muted">Age:</span> <field name="age"/>
                                    <span class="text-muted ms-2">Gender:</span> <field name="gender"/>
                                </div>
                                <div>
                                    <i class="fa fa-phone"/> <field name="phone"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    
    <!-- Action -->
    <record id="action_clinic_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">clinic.patient</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="view_clinic_patient_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first patient!
            </p>
            <p>
                Start managing patient records, medical history, and appointments.
            </p>
        </field>
    </record>
</odoo>